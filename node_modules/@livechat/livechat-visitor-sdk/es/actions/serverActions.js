import { createActionCreator } from '@livechat/redux-utils';

var INCOMING_MESSAGE = 'INCOMING_MESSAGE';

export var incomingMessage = createActionCreator(INCOMING_MESSAGE, function (_ref) {
	var name = _ref.name,
	    message = _ref.message;
	return {
		name: name,
		message: message
	};
});